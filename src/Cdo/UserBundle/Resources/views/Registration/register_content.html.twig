{% trans_default_domain 'FOSUserBundle' %}

<form action="{{ path('cdo_user_registration_register') }}" {{ form_enctype(form) }} method="POST" class="cdo_user_registration_register">
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'registration.submit'|trans }}" />
    </div>
</form>
